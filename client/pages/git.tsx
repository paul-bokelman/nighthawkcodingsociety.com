import React from "react";
import type { NextPage } from "next";
interface Props {}

const git: NextPage = (props: Props) => {
  return (
    <div>
      <h1>git</h1>
    </div>
  );
};

export default git;
