import React from "react";
import type { NextPage } from "next";
interface Props {}

const raspberrypi: NextPage = (props: Props) => {
  return (
    <div>
      <h1>raspberrypi</h1>
    </div>
  );
};

export default raspberrypi;
